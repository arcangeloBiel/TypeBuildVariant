package com.agencytravelboat.buildvariant

import splitties.resources.appStr

class ConfigBuild {
    private var baseDev: String = appStr(R.string.API_DEV)
    private var baseProd: String = appStr(R.string.API_PROD)
}